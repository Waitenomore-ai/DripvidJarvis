{
  "name": "dripvid-jarvis",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone localhost operator service for DripVid JARVIS",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/config.js"
  },
  "engines": {
    "node": ">=22"
  }
}
